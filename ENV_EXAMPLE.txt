# .env.example para backend-python
# Copia este archivo a backend-python/.env y reemplaza los valores según corresponda

# Base de datos PostgreSQL (opcional - si no está definida, usa modo memoria)
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/sir_cusco

# Orígenes permitidos para CORS (ya configurado por defecto para desarrollo local)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Nota: Si dejas estas variables vacías, el sistema funcionará en modo memoria
# lo cual es perfecto para demostración sin necesidad de PostgreSQL
